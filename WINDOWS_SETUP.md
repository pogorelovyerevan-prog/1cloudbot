# Настройка Windows сервера для автозапуска парсера ATI

## Данные для подключения
- **IP:** 80.251.156.22
- **Пользователь:** CloudAdmin
- **Пароль:** d32jk8cAHTwW4eX8

## Шаг 1: Автоматический вход пользователя (без ввода пароля)

1. Нажмите `Win + R`, введите `netplwiz` и нажмите Enter
2. Снимите галочку **"Требовать ввод имени пользователя и пароля"**
3. Нажмите **Применить**
4. Введите пароль `d32jk8cAHTwW4eX8` дважды
5. Нажмите **OK**

## Шаг 2: Автозапуск Chrome при входе в систему

### Вариант A: Через папку Автозагрузки (простой)

1. Нажмите `Win + R`, введите `shell:startup` и нажмите Enter
2. Создайте ярлык Chrome в открывшейся папке:
   - ПКМ → Создать → Ярлык
   - Путь: `"C:\Program Files\Google\Chrome\Application\chrome.exe"`
   - Имя: Chrome

### Вариант B: Через bat-файл (рекомендуется)

1. Создайте файл `C:\Users\CloudAdmin\Desktop\start_chrome.bat`:

```batch
@echo off
:: Ждём 30 секунд после загрузки системы
timeout /t 30 /nobreak

:: Запускаем Chrome
start "" "C:\Program Files\Google\Chrome\Application\chrome.exe"

echo Chrome запущен
```

2. Нажмите `Win + R`, введите `shell:startup`
3. Создайте ярлык на `start_chrome.bat` в папке автозагрузки

## Шаг 3: Перезапуск Chrome для решения Out of Memory

Создайте файл `C:\Users\CloudAdmin\Desktop\restart_chrome.bat`:

```batch
@echo off
echo Перезапуск Chrome для очистки памяти...

:: Закрываем все процессы Chrome
taskkill /F /IM chrome.exe /T 2>nul

:: Ждём 10 секунд
timeout /t 10 /nobreak

:: Запускаем Chrome заново
start "" "C:\Program Files\Google\Chrome\Application\chrome.exe"

echo Chrome перезапущен
```

### Настройка автоматического перезапуска (Task Scheduler)

1. Нажмите `Win + R`, введите `taskschd.msc`
2. **Действие → Создать задачу**
3. Вкладка **Общие**:
   - Имя: `Restart Chrome`
   - Выполнять для пользователя: CloudAdmin
   - ✅ Выполнять вне зависимости от регистрации
4. Вкладка **Триггеры** → Создать:
   - Ежедневно
   - Начать: 12:00 (полдень - перезапуск в середине рабочего дня)
   - ✅ Повторять каждые: 4 часа (или по необходимости)
5. Вкладка **Действия** → Создать:
   - Действие: Запуск программы
   - Программа: `C:\Users\CloudAdmin\Desktop\restart_chrome.bat`
6. Вкладка **Параметры**:
   - ✅ Выполнять задачу по требованию
7. Нажмите **OK**, введите пароль

## Шаг 4: Проверка работы

1. Перезагрузите Windows сервер через Telegram бот (кнопка 🔄 Перезагрузка)
2. Подождите 2-3 минуты
3. Подключитесь по RDP и убедитесь что:
   - Вход выполнен автоматически
   - Chrome запущен
   - Расширение парсера активно

## Дополнительно: Оптимизация Chrome для экономии памяти

Добавьте флаги запуска Chrome в ярлык:

```
"C:\Program Files\Google\Chrome\Application\chrome.exe" --disable-background-networking --disable-component-update --disable-default-apps --disable-extensions-except="путь_к_расширению" --disable-hang-monitor --disable-popup-blocking --disable-prompt-on-repost --disable-sync --metrics-recording-only --no-first-run --safebrowsing-disable-auto-update
```

Эти флаги отключают фоновые процессы и экономят память.

## Telegram бот - управление расписанием

После настройки Windows, расписание можно менять через Telegram:

- `/schedule` - показать текущее расписание
- `/settime 9 20` - установить время 09:00-20:00
- `/setdays 1,2,3,4,5` - установить дни (Пн-Пт)
- `/holidays` - показать ближайшие праздники

⚠️ **Важно:** После изменения расписания в боте, обновите также настройки в расширении Chrome!

